name: Code Coverage check

on:
  schedule:
    - cron: 0 3 * * *
  workflow_dispatch:
  issue_comment:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    if: ${{ github.event.type != 'IssueCommentEvent' || (github.event.type == 'IssueCommentEvent' 
      && github.event.issue.pull_request  
      && contains(github.event.issue.comment.body, '/checkCoverage')) }}

    name: Check test coverage

    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Set up JDK 17
      uses: actions/setup-java@v5
      with:
        java-version: 17
        distribution: 'temurin'
        cache: 'gradle'

    - name: Build with Gradle
      run: ./gradlew build jacocoTestCoverageVerification -PenableCodeCoverage=true

    - name: Summarize results
      run: |
        python config/transformJacocoResult.py
        cat build/reports/jacoco/test/output_table.html >> $GITHUB_STEP_SUMMARY

    - name: Comment
      env:
        PR_NUMBER: ${{ github.event.comment.body }}
        EVENT_TYPE: ${{ github.event.type }}
        GH_TOKEN: ${{ github.token }}
      run: |
        echo "$EVENT_TYPE"
        content=$(cat build/reports/jacoco/test/output_table.html)
        gh pr comment $PR_NUMBER --body "$content"

    - name: Upload coverage result
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: build/reports/jacoco/
